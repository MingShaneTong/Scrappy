package scrappy.web.instructions.parameters;

/**
 * Text to Capture
 */
public enum CaptureType {
    /**
     * Capture the HTML Elements
     */
    HTML,
    /**
     * Capture the text element within element
     */
    TEXTCONTENT
}
